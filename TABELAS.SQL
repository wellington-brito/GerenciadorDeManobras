CREATE TABLE MANOBRA
(
	ID INTEGER NOT NULL PRIMARY KEY IDENTITY,
	NOME VARCHAR(50) NOT NULL UNIQUE,
	DIFICULDADE INTEGER,
	STATUS INTEGER,
	ID_CATEGORIA INTEGER FOREIGN KEY REFERNCES CATEGORIA(ID)
);


CREATE TABLE CATEGORIA
(
	ID INTEGER NOT NULL PRIMARY KEY IDENTITY,
	NOME VARCHAR(50) NOT NULL UNIQUE,
	DESCRICAO VARCHAR(200)
);

CREATE TABLE TREINO
(
	ID INTEGER NOT NULL PRIMARY KEY IDENTITY,
	IDMANOBRA INTEGER INTEGER FOREIGN KEY REFERNCES MANOBRA(ID),
	PROGRESSO INTEGER,
	QNTDDIAS INTEGER
);

CREATE TABLE REQUISITO
(
	ID INTEGER NOT NULL PRIMARY KEY IDENTITY,
	IDMANOBRARECENTE INTEGER,
	IDMANOBRAREQUISITO INTEGER FOREIGN KEY REFERNCES MANOBRA(ID)
);


--CREATE TABLE 
--(
--	MATRICULA INTEGER NOT NULL PRIMARY KEY,
--	NOME VARCHAR(50) NOT NULL,
--	CPF CHAR(14) NOT NULL UNIQUE,
--	DATA_NASCIMENTO DATE NOT NULL,
--	SEXO CHAR(1) NOT NULL,
--	TELEFONE VARCHAR(20) NULL,
--	ENDERECO VARCHAR(100) NOT NULL, 
--	NOTA_INGRESSO INTEGER NOT NULL,
--	ID_CURSO INTEGER NOT NULL REFERENCES CURSO(ID)
--);
